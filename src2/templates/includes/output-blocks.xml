{% macro outputBlockData(contextData) %}
    {% for loopBlock in contextData %}
    {% for blockId, blockData in loopBlock %}

    {% set blockName = blockId|blockNameFromId %}
    {% for data in blockData %}

        {% include './output-block.xml' %}

    {% endfor %}
    {% endfor %}
    {% endfor %}

{% endmacro %}

{% macro test(blockData) %}
{{blockData|dump}}
{% endmacro %}
